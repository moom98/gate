# Adapter Configuration

# Broker HTTP URL (default: http://localhost:3000)
BROKER_URL=http://localhost:3000

# Broker authentication token (optional, required after Step 7)
# BROKER_TOKEN=your-token-here

# Claude CLI command (default: claude)
CLAUDE_COMMAND=claude

# Working directory for Claude CLI (default: current directory)
# CLAUDE_CWD=/path/to/working/directory
